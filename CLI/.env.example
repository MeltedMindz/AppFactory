# App Factory CLI Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Anthropic API Key
# =============================================================================
# Get your API key from https://console.anthropic.com/
# The key should start with "sk-ant-"
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# =============================================================================
# OPTIONAL: Model Configuration
# =============================================================================
# Claude model to use for generation
# Options:
#   - claude-sonnet-4-20250514 (default, recommended for cost/quality balance)
#   - claude-opus-4-20250514 (highest quality, more expensive)
#   - claude-haiku-3-20250514 (fastest, least expensive)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Maximum tokens for API responses
# Default: 16000 (sufficient for most stages)
# Increase for Stage 10 if generating large apps
APPFACTORY_MAX_TOKENS=16000

# Temperature for generation (0.0 - 1.0)
# Default: 0.3 (more deterministic)
# Increase for more creative/varied outputs
APPFACTORY_TEMPERATURE=0.3

# =============================================================================
# OPTIONAL: Output Configuration
# =============================================================================
# Override the default output root directory
# By default, outputs go to the_factory/runs/ and the_factory/builds/
# Uncomment to use a custom location
# APPFACTORY_OUTPUT_ROOT=/path/to/custom/output

# =============================================================================
# OPTIONAL: Debug Mode
# =============================================================================
# Enable debug logging (shows detailed API calls, timing, etc.)
# Set to "true" to enable
# DEBUG=true
